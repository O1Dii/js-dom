// import { isBoolean, isNumber } from '../utils';

const toggle = function () {
  // if (isBoolean(display)) {
  //     this.each((item) => {
  //         if (display) {
  //             item.style.display = '';
  //         }
  //         else {
  //             item.style.display = 'none';
  //         }
  //     });
  // }
  // else if (isNumber(display)) {
  //     const interval = setInterval(() => {
  //         this.each((item) => {
  //         })
  //     }, 40);

  //     setTimeout(() => {
  //         clearInterval(interval);
  //     }, display);
  // }
};

export default toggle;